@model EventBLib.Models.Event

<a class="event-a" asp-controller="Events" asp-action="Details" asp-route-id="@Model.EventId">
    <div class="eventlist-container flex-vss transition-1">
        <div class="eventlist-img">
            <div class="image-container">
                <!--<img src="@Model.MediumImage" class="image-self-back" alt="event" />-->
                <img src="@Model.MediumImage" class="image-self-front" alt="event" />
                <!--<div class="image-watersign">@Model.AgeRestString</div>-->
            </div>
        </div>
        <div class="eventlist-info ">
            <div>
                <h3 class="eve-title ">@Model.Title</h3>
            </div>
            <div>
                <div class="flex-hsbc">
                    <div class="flex-hsc">
                        <span class="material-icons eve-icons">calendar_today</span>
                        <h4 class="eve-time">@Model.Date.ToString("dd MMMM")</h4>
                    </div>

                    <div class="flex-hsc">
                        <span class="material-icons eve-icons">schedule</span>
                        <h4 class="eve-time">@Model.Date.ToString("HH:mm")</h4>
                    </div>
                </div>
                <hr>
            </div>
        </div>
        <!--
    <div class="eventlist-bottom-menu">
        <div class="eventlist-btn-come-right-col">
            <img class="info-img" title="Просмотров" src="~/resourses/eye.png" />
            <span class="info-num">@Model.Views.ToString()</span>
            <img class="info-img" title="Пойдут" src="~/resourses/share.png" />
            <span class="info-num">@Model.Vizits.Count()</span>
        </div>
    </div>
        -->
    </div>
</a>